class ConfigBot {
    companion object {
        // config do Bot
        val nameBot = "Doura"
        val createdIn = 2022
        var satusConfig = false
        var userName: String = " Paim"

        // Entrada/input
        fun inputBot(): String {
            print("> ")
            return readln()
        }
    }
}